sudo: required
services:
  - docker

before_install:
  - docker build -t katarinacimesa/docker-react -f Dockerfile.dev .


script:
  - docker run -e CI=true katarinacimesa/docker-react npm run test -- --coverage
